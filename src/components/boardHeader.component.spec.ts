import BoardHeaderComponent from './boardHeader.component';

describe('BoardHeaderComponent', () => {
    it('can be created', () => {
        const sut = new BoardHeaderComponent();
        expect(sut).toBeDefined();
    });
});
