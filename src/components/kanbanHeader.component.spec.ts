import KanbanHeaderComponent from './kanbanHeader.component';

describe('KanbanHeaderComponent', () => {
    it('can be created', () => {
        const sut = new KanbanHeaderComponent();
        expect(sut).toBeDefined();
    });
});
